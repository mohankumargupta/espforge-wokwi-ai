# Gotchas

Concrete, previously-observed failure modes in generated Wokwi custom chips.
Each markdown in this directory notes the device, the symptom, the root cause, and the fix — so
future chip implementations (and their test suites) can check for the same
class of bug before it ships.
